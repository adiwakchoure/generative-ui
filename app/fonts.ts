import { Forum, Raleway } from 'next/font/google'
 
export const raleway = Raleway({
  subsets: ['latin'],
  display: 'swap',
  weight: '400',
})

export const forum = Forum({
  subsets: ['latin'],
  display: 'swap',
  weight: '400',
})
